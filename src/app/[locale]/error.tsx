'use client';
import ErrorComponent from '@/component/error_component';
import React from 'react'

type Props = {}

function Error({}: Props) {
  return (
    <div>
        <ErrorComponent/>
    </div>
  )
}

export default Error