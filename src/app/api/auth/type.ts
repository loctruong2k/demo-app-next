export interface FormLogin {
    username: string;
    password: string;
}

export interface LoginData {
    id: string,
    username: string;
    email: string;
    token: string;
}

export interface FormRegister {
    username: string;
    password: string;
    email: string;
}